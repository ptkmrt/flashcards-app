// Description of an individual score recording, containing
// person's name, the flashcard deck's name, and their rounded score
export type Score = {
    user: string,
    name: string,
    score: string
};